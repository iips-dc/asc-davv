class ApplicationController < ActionController::Base
  protect_from_forgery
end

class Shortterm_courses < ActiveRecord::Base
  validates :Name, presence: true
end
 
Shortterm_courses.create(Name: "John Doe").valid? # => true
Shortterm_courses.create(Name: nil).valid? # => false
