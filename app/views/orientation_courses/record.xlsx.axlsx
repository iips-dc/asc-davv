wb = xlsx_package.workbook
wb.add_worksheet(name: "List") do |sheet|
  i=0
  sheet.add_row ["S.no", "Name", "Email", "University", "Date Of Birth", "Mobile", "Residential Address", "Course"]
  @orientation_courses.each do |orientation_course|
  	sheet.add_row [i+=1, orientation_course.name, orientation_course.email, orientation_course.university, orientation_course.dob, orientation_course.mobile, orientation_course.res_address, orientation_course.course.course_name]
  end
  
end