<!-- row -->            
<div class="row">
    <div class="col-lg-12">
        <h1 class="page-header">Individual Record</h1>
    </div>
    <!-- /.col-lg-12 -->
</div>
<!-- /.row -->

<div class="col-md-offset-1 col-md-10 col-md-offset-1">
    <%= render 'detail' %>

    <%= link_to 'Edit', edit_winter_school_path(@winter_school), :class => 'btn btn-default' if can? :update, WinterSchool%>
    <%= link_to 'Delete', winter_school_path, method: :delete, :class => 'btn btn-default', data: { confirm: 'Are you sure?' } if can? :destroy, WinterSchool %> 
    <%= link_to 'Back', winter_record_path, :class => 'btn btn-default' %>
    <%= link_to "Download as Excel", winter_school_path(format: "xlsx"), :class => 'btn btn-default' %>
</div>
<!-- /. offset -->
